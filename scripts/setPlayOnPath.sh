#!/bin/zsh
export PATH=/Users/michaelv/work/playground/play/play-2.2.1:$PATH
# for some reason, running this script does not update my PATH variable (probably to do with using zshell). Just run export manually in terminal to get access to play binary
